{
  "name": "azul-github-isssue-labeller",
  "version": "1.0.0",
  "private": true,
  "description": "Automatically apply a label to new Github issues in a repository",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc && npm prune --production && git add -f node_modules lib package-lock.json && npm install",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/HumanCellAtlas/azul-github-labeler-action.git"
  },
  "keywords": [
    "actions",
    "node",
    "setup"
  ],
  "author": "HumanCellAtlas",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/github": "^1.0.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "@types/node": "^12.0.4",
    "jest": "^24.8.0",
    "jest-circus": "^24.7.1",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.1"
  }
}
